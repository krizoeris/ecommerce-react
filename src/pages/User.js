import React, { useState, useContext } from 'react'
import AppContext from '../AppContext'

const User = () => {


  return(
    <div>
    {/* check if user is logged in */}
    {/* IF not logged in, register option */}
    If logged in, 



    </div>
  )
}